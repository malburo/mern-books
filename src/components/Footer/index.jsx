import { Layout } from 'antd';
import React from 'react';
import './style.scss';
const { Footer } = Layout;
FooterContainer.propTypes = {};

function FooterContainer(props) {
  return <Footer style={{ textAlign: 'center' }}>Created by malburo</Footer>;
}

export default FooterContainer;
